export interface DelFolderParams {
  userId: string;
  folderId: string;
}

export interface UserId {
  userId: string;
}

export interface SourcePriorityUpdate {
  source_id: number;
  priority: number;
}
